# Rust Rewrite — Slice Plan

Generated by workflow `rust-rewrite-slice-plan` (8 agents, adversarially verified 2026-09-22), revised after verdicts.

**AMENDED 2026-09-22 (shell decision — see docs/research/rust-rewrite-decisions.md):** shell = napi-rs into existing Electron, NOT Tauri. s12–s16 titles mentioning Tauri/WebviewWindow/tauri.conf.json read as their napi/Electron equivalents: s12 = about-window parity via existing Electron window + Rust state; s13 = protocol handlers stay `protocol.handle`, Rust serves content via napi; s14 = frontend bridge stays window.bxApi shape backed by napi exports; s15 = keep electron-builder packaging, add `.node` smartUnpack verification; s16 = CI adds cargo job (build/clippy/test) alongside existing. Crates s02–s11 unchanged.

Machine data: `docs-rust-plan.json` (root). Discipline: one slice = one PR = maker → independent verifier → human merge. Loop-engineering refactor path.

## Slices

| # | ID | Effort | Depends on | Title |
|---|----|--------|-----------|-------|
| 1 | s01-ws-scaffold | S | — | Cargo workspace scaffold: `rust/` root, empty lib crates (station-sdk, manifest-registry, activity, appstore-schema) + station-shell bin stub. Verify: `cargo check --workspace && cargo clippy --workspace -- -D warnings` |
| 2 | s02-sdk-core | M | s01 | SDK core: common base + facade + leaf consumers (storage, config, history, ipc, resources, session) as typed traits/state over tokio sync primitives |
| 3 | s03-sdk-streams | M | s02 | SDK stream consumers: search + activity push/query over tokio-stream |
| 4 | s04-sdk-tabs-react-surface | M | s02 | SDK tabs + react consumer trait surface (webview ops as trait methods, no impl) |
| 5 | s05-ts-dead-code | S | — | Delete dead TS: `packages/app/src/api/websocket.ts` + `ci/tsc.sh`. Verify: rg no matches |
| 6 | s06-activity-store | M | s01, s03 | Activity store: sqlx SQLite schema + push/query/merge (frecency port). Verify: `cargo test -p activity` (sqlite::memory:) |
| 7 | s07-manifest-model | M | s01 | Manifest model: serde BxAppManifest + embed JSON via include_dir. Verify: parsed count == JSON file count |
| 8 | s08-manifest-search-private | M | s07 | Fuzzy search + private manifests. **Verify: author golden-query fixtures FIRST** (none exist — verifier finding) |
| 9 | s09-manifest-config-rules | M | s07 | isConfigurationRequired + handlebars templating. **Verify: author golden cases FIRST** (helpers.ts has 0 tests) |
| 10 | s10-appstore-schema-types | M | s01 | Copy SDL verbatim + serde types + UI-state replacing Apollo cache. Verify: `diff` SDL empty + `cargo test -p appstore-schema` |
| 11 | s11a-appstore-read-commands | M | s06, s08, s09, s10 | Read-only commands: search/popular/categories over manifest-registry (split from old s11 — too big) |
| 12 | s11b-appstore-write-commands | M | s11a | Install/uninstall/request-private orchestration + saga/duck replacement + mock deletion |
| 13 | s12-about-window | S | s01 | About window lifecycle. Verify: compile + acceptance note (blur/Escape manual — no mechanical check exists) |
| 14 | s13-shell-protocols | M | s01, s10 | `appstore` protocol handler + `station://` schemes |
| 15 | s14-appstore-ui-wiring | M | s11b, s13 | Remove window.bxApi bridge + mocks. Verify: rg `bxApi\|mocked` repo-wide (wide pattern — narrow one evaded aliases) |
| 16 | s15-packaging-tauri | L | s12, s14 | tauri.conf.json, bundler targets, icons from `packages/app/build/`, **also covers: electron-builder.yml, webpack.config.renderer.js, entitlements.mac.plist, abstract-application build residue** |
| 17 | s16-ci-rust | M | s15 | CI: build/clippy/test + rust-cache. **Also covers: root package.json scripts, scripts/sdk.ts, dev_utils/userDataManager.ts** |

## Coverage fixes from verify pass

- Old s11 split → s11a/s11b (too big).
- Orphans assigned: `abstract-application/{duck,resolvers,sagas,types}.ts, queries@local.gql` → s16 residue/s15; `api/logger.ts`, `appstore/src/shared/constants/constants.ts`, `manifests/icons/` → nearest slice (s16/s15/s07); `scripts/sdk.ts`, `dev_utils/` → s16.
- Goldens authored in-slice for s08/s09 (first step, before port).
- s14 verification widened to repo-wide `bxApi|mocked` grep.

## Workspace layout

```
rust/
  Cargo.toml            # [workspace] members = ["crates/*"]
  rust-toolchain.toml   # pinned stable
  crates/
    station-sdk/        # from packages/sdk
    manifest-registry/  # from packages/app/manifests + abstract-application helpers
      definitions/      # JSON manifests verbatim, include_dir
    activity/           # from packages/app/src/activity
      migrations/       # sqlx
    appstore-schema/    # from packages/appstore graphql data layer
      schema/api.graphqls
    station-shell/      # Tauri bin — last slices land here
      tauri.conf.json
```

## Open questions — RESOLVED 2026-09-22

Research: `docs/research/rust-rewrite-decisions.md` (primary sources). Highlights:

1. **Shell: sequenced hybrid.** s02–s11 crates land shell-agnostic. Bind into existing Electron main via **napi-rs** (v3, ABI-stable, smartUnpack already compatible), delete TS services slice-by-slice. Tauri deferred: multiwebview still `unstable`-flagged, WebKitGTK child-webview broken (#10131 #10420). s12/s13 become napi-binding + window-lifecycle parity slices; s15/s16 keep Electron packaging unless Tauri stabilizes. Repo fact: 0 BrowserView hits — tabs are `<webview>` tags.
2. **Updates: keep electron-updater** under retained Electron shell. Channels work today (prerelease tiers / channel-suffixed latest.yml). canary.js = release choreography, replace with `gh` CI steps, do not port. Tauri path (if later): tauri-plugin-updater, per-channel `latest.json` endpoints. cargo-dist/axo rejected.
3. **Notarization: gated on Apple Developer Program ($99/yr + Developer ID cert), not tooling.** If acquired: Tauri = env-var config in s15 (~half day); Electron = uncomment afterSign + ASC-key auth (altool-era script is dead, TN3147). Until then: drop mac from release matrix rather than ship unsigned.
4. **Fuzzy search: nucleo-matcher** (s08). `Pattern::parse(...).match_list()` = scored sorted fuzzy over manifest names. Outperforms alternatives; Helix's engine.
